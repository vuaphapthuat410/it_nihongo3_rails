

<head>
   <title>Welcome </title>
   <style>
      .box {
         margin-left: 30px;
      }
      .link_box {
         display: flex;
         align_items: center;
         margin-top: 30px;
      }
      .line {
         margin-top: 10px;
         border-bottom: 2px solid grey;
      }
   </style>
</head>

<body>
   <div class="box">
      <div class = "link_box">
         <% if user_signed_in? %>
           <%= link_to('ログアウト', destroy_user_session_path, method: :delete) %>        
         <% else %>
           <%= link_to('サインイン処理', new_user_session_path) %> 
         <% end %>
         &nbsp;&nbsp;
         <%= link_to('ユーザ登録', new_user_registration_path) %>
      </div>
      <div class="line">
      </div>
      <h1>HOME PAGE</h1>
      <% if user_signed_in? %>
        <h2><%= link_to('本一覧を見る', "books") %></h2>    
      <% end %>
   </div>
</body>